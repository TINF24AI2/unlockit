info:
  name: Get Available Licenses
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/license-keys"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
