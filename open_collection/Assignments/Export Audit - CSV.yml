info:
  name: Export Audit - CSV
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/api/audit/export"
  params:
    - name: format
      value: csv
      type: query
      disabled: false
    - name: startDate
      value: ""
      type: query
      disabled: true
    - name: endDate
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
