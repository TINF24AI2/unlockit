info:
  name: Get Assignments
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/assignments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
