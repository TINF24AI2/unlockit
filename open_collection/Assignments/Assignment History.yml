info:
  name: Assignment History
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/audit/assignments"
  params:
    - name: id
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
