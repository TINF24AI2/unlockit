info:
  name: Get Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/users/users"
  body: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
