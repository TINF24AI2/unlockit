info:
  name: Login
  type: http
  seq: 6

http:
  method: POST
  url: "{{host}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "admin",
        "password": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
