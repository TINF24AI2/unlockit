info:
  name: Set Password
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/api/auth/set-password"
  body:
    type: json
    data: |-
      {
        "token": "ff...",
        "password": "verySecure12"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
