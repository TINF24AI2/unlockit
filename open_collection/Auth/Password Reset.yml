info:
  name: Password Reset
  type: http
  seq: 8

http:
  method: POST
  url: "{{host}}/api/auth/password-reset"
  body:
    type: json
    data: |-
      {
        "email": "jdoe@mail.com",
        "oldPassword": "SecurePassword123!",
        "newPassword": "NotSoSecure"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
