info:
  name: Get Licenses
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/license-keys"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
