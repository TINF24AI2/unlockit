info:
  name: Create License Key
  type: http
  seq: 5

http:
  method: POST
  url: "{{host}}/api/license-keys"
  body:
    type: json
    data: |-
      {
        "productId": "{{productId}}",
        "licenseName": "Standard License",
        "licenseKey": "ABC-123-DEF",
        "licenseType": "SINGLE",
        "maxUsages": 1,
        "requiresAdminApproval": true,
        "expiresAt": null
      }
  auth: inherit

runtime:
  scripts:
    - type: pre-request
      code: |-
        const productId = bru.getEnvVar('productId');
        if (!productId) {
          console.warn('WARNING: productId is not set. Did you run "Create Product" first?');
        } else {
          console.log('Using productId:', productId);
        }
    - type: after-response
      code: |-
        const body = res.getBody();
        if (body?.data?.id) {
        bru.setVar('licenseId', body.data.id);
        bru.setEnvVar('licenseId', body.data.id);
        bru.setGlobalEnvVar('licenseId', body.data.id);
        console.log('All vars set:', body.data.id);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
