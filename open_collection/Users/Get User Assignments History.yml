info:
  name: Get User Assignments History
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}/api/users/history"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
