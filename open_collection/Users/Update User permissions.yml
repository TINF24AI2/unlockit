info:
  name: Update User permissions
  type: http
  seq: 6

http:
  method: POST
  url: "{{host}}/api/users/:change/:id"
  params:
    - name: change
      value: permissions
      type: path
    - name: id
      value: 3
      type: path
  body:
    type: json
    data: |-
      {
        "permissions": ["ADMIN"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
