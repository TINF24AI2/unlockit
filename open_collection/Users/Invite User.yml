info:
  name: Invite User
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/api/users"
  body:
    type: json
    data: |-
      {
        "email": "test@tix4u.de",
        "username": "John Doe",
        "admin": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
