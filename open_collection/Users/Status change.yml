info:
  name: Status change
  type: http
  seq: 5

http:
  method: POST
  url: "{{host}}/api/users/status"
  body:
    type: json
    data: |-
      {
        "status": "DEACTIVATED",
        //"status": "ACTIVE",
        //"status": "DELETED"
        "id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
